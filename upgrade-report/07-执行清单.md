# 07 · 执行清单（可回归跟踪）

> 项目：Codex Remote Bridge / Chat 远程连接 v4.0.0
> 日期：2026-08-06 ｜ 依据：FINAL_UPGRADE_REPORT.md + upgrade-report/02/03/05/06
> 门禁：`npm run check`、`npm test`、`node server.js` 冒烟、响应式 DOM 检查、新增安全/性能测试
> 说明：历史报告日期为 2026-08-04，是审计输入；以当前源码与测试（64/64）为准。

---

## 基线（已核实）

| 项 | 结果 |
|---|---|
| `npm test` | 64/64 通过（2026-08-06 实测） |
| `npm run check` | 通过（2026-08-06 实测） |
| 最终门禁 | 2026-08-06 完成时 `npm test` 75/75、`npm run check` 通过（P2 新增 11 项测试见下表） |
| 已有能力（禁止重复实现） | 语音输入/输出、Markdown 导出、提示词库、全文搜索、时区、/codex/status 增量解析、线程列表 3s TTL、P50/P95 延迟统计 |

---

## P0 · Web/PWA 高感知体验升级

| # | 发现 | 决策 | 任务 | 验证结果 |
|---|---|---|---|---|
| P0-1 | `:root` 已含 64+ 设计 token，双主题 + black-sky 覆盖完整；危险/骨架屏/焦点环无语义 token | 仅新增必要 token：`--danger-text`、`--skeleton-bg/hi`、`--focus-ring`；不重写 | 在 dark/light 主题定义处补齐 token，替换高危重复色 | 通过：三个 token 均在 dark(:root) 与 white-night 双主题定义并被引用（index.html L47-53、L2279-2281） |
| P0-2 | 线程菜单加载仅 15px spinner，无骨架；聊天历史用文字"正在加载" | 新增线程列表骨架行 + 消息骨架气泡（shimmer，reduced-motion 降级静态） | 实现骨架屏与状态渲染函数 | 通过：`threadListBusy` 渲染 4 条 `.thread-skeleton-row`（role=status）；`appendMessageSkeleton()` 渲染 `.message.is-skeleton`（role=status）；reduced-motion 下 shimmer 停用（L3660-3662） |
| P0-3 | 线程列表失败只弹 notice，菜单内无错误态；离线仅顶栏小 badge | 新增线程菜单错误态（含重试）+ 离线横幅（含重试） | 实现状态组件与接线 | 通过：菜单错误态 `.thread-menu-error`（标题+详情+重试，aria-label=重试）；离线横幅 offline/connecting/online 三态显隐与文案、重试按钮可见性全部断言通过 |
| P0-4 | 长列表（线程/消息/工作区行）未用 `content-visibility` | 仅在 `@supports` 下启用，配 `contain-intrinsic-size`，避免滚动回归 | 追加 CSS 规则 | 通过：`@supports (content-visibility: auto)` 下 `.thread-option/.message/.workspace-row` 启用并配 `contain-intrinsic-size: auto 60px`（L3758-3761） |
| P0-5 | 抽屉（thread-menu）无焦点管理：打开不聚焦、Esc 不关闭、关闭不归还焦点 | 打开聚焦搜索/首元素，Esc 关闭，关闭归还焦点 | 实现 focus trap 辅助函数 | 通过：393px 与 768px 打开聚焦 `#thread-search`、Esc 关闭、焦点归还 `#thread-button`；≥900px rail 模式菜单常显不覆盖；真实键盘 Tab 后 `:focus-visible`=true、outline 2px solid |
| P0-6 | 移动端 composer 在 393px 下有挤压风险（徽章+输入+图标） | 补 ≤393px 收敛规则（徽章紧凑、输入 min-width、安全区） | 追加媒体查询 | 通过：393px 下 `scrollWidth=clientWidth=393` 无横向溢出，composer 位于视口内；安全区 `env(safe-area-inset-bottom)` 已用于输入区（L2764）；图标按钮 7/7 均有 aria-label/title |

### P0 验证期修复（基线审计发现，非新增功能）
| 问题 | 修复 |
|---|---|
| `appendMessageSkeleton()` 误嵌在 `loadThreadHistory()` 函数体内，全局不可见 | 移出为独立顶层函数（脚本方式移动，保留 CRLF/LF 混排区域） |
| `updateOfflineBanner()` 误嵌在 `renderPetBuddy()` 函数体内，`setRouteState` 调用会抛 ReferenceError | 移出为独立顶层函数；修复后 offline/connecting 横幅按状态渲染正常 |

---

## P1 · 桌面与原生壳统一

| # | 发现 | 决策 | 任务 | 验证结果 |
|---|---|---|---|---|
| P1-1 | WPF MainWindow.xaml ~38 处内联 hex | 收敛为命名 Brush 资源（组件级） | 逐资源抽取，保持语义 | 通过：`dotnet build` 0 警告 0 错误；组件级硬编码色收敛为命名 Brush（服务状态/二维码入口/日志/操作按钮层级统一） |
| P1-2 | Android WebView 失败仅 Snackbar | 增加内嵌失败视图（重试/回退连接页），Compose 色板沿用 | 修改 BrowserScreen.kt + ConnectionStateViewModel.kt + strings.xml | 通过：`:app:assembleDebug` BUILD SUCCESSFUL；层内失败态（加载失败/连接失败/重试/回连接页）就位 |
| P1-3 | macOS ContentView.swift 色值散落 | 新增私有 Color 扩展集中色板 | 修改 ContentView.swift + ServiceManager.swift | 通过：静态审查（本机无 Swift 工具链，无法编译）；色值收敛到私有 Color 扩展，与 Web/Android 语义一致（成功/错误/警告/信息） |

---

## P2 · 速度、稳定性与核心功能补强

| # | 发现 | 决策 | 任务 | 验证结果 |
|---|---|---|---|---|
| P2-1 | 历史大文件双重 JSON.parse（03-报告热点 B） | 合并单遍扫描（达到 limit 即停） | 重构 handleThreadHistory 计数与解析 | 通过：`scanCodexThreadHistoryLines` 单遍扫描 + `readHistoryTailLines` 自适应读取（初始 8MB，不足扩到 128MB）；test/history.test.js 2 项通过（真实 HTTP 回归 + 源码断言）。基准（`.bench-history.ps1`，旧 HEAD vs 新工作树）：64MB dense median 14.9→14.8ms、20MB sparse median 15.5→14.4ms，稳态持平；sparse 7 次采样中前 2 次大幅提速（7→1.2ms），无回归 |
| P2-2 | 无受控文件下载 | 新增 `/codex/download`：白名单目录 + 路径穿越防护 + 大小上限 | server.js + 前端入口 + 测试 | 通过：`handleFileDownload`（cwd 白名单 = existingProjectCwds、`../`/绝对路径 403、非普通文件/符号链接 403、>MAX_DOWNLOAD_BYTES 20MB 413、缺失 404、未授权 401、流式响应带 Content-Disposition）；`capabilities.canDownload`；test/download.test.js 2 项通过。E2E 下载入口三档验收（`.e2e-download-entry.ps1`，合成 `canDownload=true` 打开环境面板）：393x852 / 768x1024 / 1280x800 三档 `#environment-download-path`（aria-label「工作区内文件相对路径」、placeholder「例如 src/app.js」）与 `[data-env-action="download"]`（文案「下载」）均存在且 rect 非零、`activeWorkspace=settings`、无横向溢出（doc/body/panel 三处均 false） |
| P2-3 | token 无只读/读写区分 | 新增 scope 字段，写接口拒绝只读 token | security + 路由校验 + 测试 | 通过：src/security/sessions.js 会话带 scope；`readOnlyToken()` 派生 `:ro`、`tokenScopeFromRequest()` 解析（会话优先）；非幂等写方法（POST/PUT/PATCH/DELETE 等）403 `TOKEN_READ_ONLY`；`/codex/config` 返回 `scope`；test/scope.test.js 4 项通过（主 token 读写、`:ro` 只读、会话降级、写拒绝）。前端只读态 CDP 验收：只读 token 打开 393x852 下 body.is-readonly、#send/#approval-once/#keep-awake/#new-thread disabled、#text.readOnly + placeholder「只读模式，无法发送」、只读横幅可见、badge `is-local is-readonly`；sendText/submitApprovalDecision/postThreadAction 与捕获阶段 click 拦截均弹出只读 notice 且不发请求；读写 token 页无 is-readonly、控件可用；393/768/1280 三档无横向溢出、顶栏 62px 稳定 |
| P2-4 | 截图预览缺失 | CDP 可用时 `/codex/screenshot`，带降级态/限流/缓存/鉴权 | server.js + 前端 + 测试 | 通过：`handleScreenshot` 未授权 401、CDP 不可用 503 `CDP_UNAVAILABLE`、POST 405、缓存 TTL/`X-Screenshot-Cache` 头、`browser.screenshot.available/maxAgeMs` 暴露；test/screenshot.test.js 2 项通过。E2E：真实打开环境面板（`openEnvironmentMenu` → settings/environment）显示「CDP 不可用，无法预览截图…」；合成 503 降级态 `.environment-screenshot-state.is-degraded` + role=alert + 无横向溢出（393x852） |
| P2-5 | 定时任务 UI 缺少 run-now 等操作 | 补可操作 UI（运行中/失败/重试状态更清楚） | 前端 scheduled workspace | 通过：run/remove/toggle 挂 `data-schedule-action`；running/started 禁用 run；chip `data-schedule-status`（running/started/error/skipped/paused→已暂停）；error+lastMessage 渲染 `.workspace-row-error` role=alert；test/schedule-ui.test.js 1 项通过。E2E（注入 5 类假任务）：393/768/1280 三档 5 行、chip 状态 5/5 正确、run 禁用 [true,true,false,false,false]、error 行 role=alert 文案正确、无横向溢出（doc/body/panel）、顶栏 `.top-actions` 高 34px 稳定；只读点击拦截 notice「只读模式：该操作需要读写令牌」且 confirm 未触发 |

---

## 阶段验收

| 阶段 | npm run check | npm test | 起服冒烟 | DOM/响应式 | 新增测试 |
|---|---|---|---|---|---|
| P0 | 通过 | 64/64 通过 | 通过（`/` 200、`/codex/health` 200、无 token `/codex/stats` 401） | 通过（393x852 / 768x1024 / 1280x800 三档：无横向溢出、composer/工具栏稳定、抽屉焦点管理、骨架/错误/空态/离线横幅） | —（P0 无服务端新增行为，前端以 DOM/CDP 验收代替） |
| P1 | 通过 | 66/66 通过（P2-1 起累加） | 通过（同 P0） | —（原生壳，无 DOM） | — |
| P2（进行中） | 通过 | 68/68 通过（含 P2-1/P2-2 新增 4 项） | 待跑 | 待跑（下载入口 393/768/≥900 三档） | test/history.test.js + test/download.test.js |
| P2（进行中） | 通过 | 72/72 通过（含 P2-1/P2-2/P2-3 新增 8 项） | 通过（`/` 200、`/codex/health` 200、无 token `/codex/stats` 401、`/codex/config` scope 正确） | 通过（只读/读写两态 × 393/768/1280 三档：无横向溢出、工具栏稳定、只读禁态与拦截、焦点规则在位） | test/history.test.js + test/download.test.js + test/scope.test.js |
| P2（完成） | 通过 | 75/75 通过（P2 累计新增 11 项：history 2 + download 2 + scope 4 + screenshot 2 + schedule-ui 1） | 通过（生产默认端口 8787 起服：`/` 200 且 PWA 在位、`/codex/health` 200、无 token `/codex/stats` 401、`/codex/config` scope=read-write、`/codex/screenshot` 503 `CDP_UNAVAILABLE`、`/codex/download` 真实下载 `package.json` 200 + Content-Disposition、`..` 穿越 403、绝对路径 403、无 token 401） | 通过（P2-4 环境面板 CDP 降级态 × 393；P2-5 定时任务 5 状态 chip/禁用/error alert × 393/768/1280 无横向溢出、顶栏 34px 稳定；只读拦截 notice；下载入口三档验证见 P2-2） | test/history.test.js + test/download.test.js + test/scope.test.js + test/screenshot.test.js + test/schedule-ui.test.js |
